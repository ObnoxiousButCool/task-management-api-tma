# PostgreSQL connection string used by Flask-SQLAlchemy.
DATABASE_URL=postgresql://tma_user:tma_password@localhost:5432/tma

# Secret used to sign JWT access tokens. Replace this in every environment.
JWT_SECRET_KEY=replace-with-a-long-random-secret

# bcrypt work factor. Use 12 or higher outside tests.
BCRYPT_ROUNDS=12
